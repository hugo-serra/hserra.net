{
  "installCommand": "mkdir -p ~/.ssh && echo \"$DESIGN_SSH_KEY\" | tr -d '\\r' > ~/.ssh/id_ed25519 && chmod 600 ~/.ssh/id_ed25519 && ssh-keyscan -H github.com >> ~/.ssh/known_hosts 2>/dev/null && bun install"
}
